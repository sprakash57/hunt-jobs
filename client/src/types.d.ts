interface SearchFields {
    query: string;
    location: string;
}
interface Job {
    id: string;
    title: string;
    company: string;
    description: string;
    location: string;
    publishedDate: string;
}